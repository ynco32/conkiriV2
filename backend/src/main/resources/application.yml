spring:
  profiles:
    active:
      - local
    group:
      local:
        - oauth
        - db-local
        - cloud
        - redis
        - rabbitmq-local
        - fcm
      master:
        - oauth
        - db-master
        - cloud
        - redis
        - rabbitmq
        - fcm
    include: oauth, db, cloud, redis, rabbitmq, fcm

logging:
  level:
    root: INFO
    com.conkiri: DEBUG
